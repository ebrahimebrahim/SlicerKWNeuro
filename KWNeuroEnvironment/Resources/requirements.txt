# Runtime Python dependencies installed on demand from the KWNeuroEnvironment widget.
# Intentionally empty — this module's *purpose* is to drive pip installs of
# kwneuro + the bridge + the five optional extras, so there are no extras
# for Slicer's own pip_ensure scaffolding to install up front.
