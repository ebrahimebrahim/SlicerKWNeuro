# Brain extraction requires kwneuro[hdbet]. KWNeuroBrainExtract checks
# this explicitly via kwneuro_slicer_bridge.ensure_extras_installed and
# instructs the user to enable the extra in KWNeuroEnvironment if absent.
